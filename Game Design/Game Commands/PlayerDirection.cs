using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// PlayerDirection is an enum used
/// to determine the direction of a player.
/// Default direction is NONE.
/// </summary>
public enum PlayerDirection
{
    NONE, 
    UP,
    DOWN,
    LEFT,
    RIGHT
}